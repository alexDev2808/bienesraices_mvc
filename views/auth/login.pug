extends ../layout/index

block contenido
    div.py-10
        h1(class="text-4xl my-10 font-extrabold text-center") Bienes
            span.font-normal Raices
        h2(class="text-center font-extrabold text-2xl")= pagina

        if errores 
            div(class="max-w-md mx-auto my-10")
                each error in errores 
                    p(class="bg-red-600 text-white uppercase text-xs text-center p-2 mb-1 font-bold")= error.msg


        div(class="mt-8 mx-auto max-w-md")
            .bg-white.py-8.px-4.shadow

                form(class="space-y-5" method="POST" action="/auth/login")
                    input(type="hidden" name="_csrf" value=csrfToken)
                    div
                        label(for="email" class="block text-sm uppercase text-gray-500 mb-2 font-bold") Email de Registro
                        input(id="email" class="w-full px-3 py-2 border border-gray-300 rounded-md placeholder-gray-400" placeholder="Tu Email" type="email" name="email") 

                    div
                        label(for="password" class="block text-sm uppercase text-gray-500 mb-2 font-bold") Password
                        input(id="password" class="w-full px-3 py-2 border border-gray-300 rounded-md placeholder-gray-400" placeholder="Tu Password" type="password" name="password") 

                    div(class="flex items-center justify-between")
                        a(href="/auth/registro" class="text-gray-500 text-xs") No tienes cuenta? Crea una
                        a(href="/auth/olvide-password" class="text-gray-500 text-xs") Olvide mi Password


                    input(class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 rounded-md" type="submit" value="Iniciar sesion")